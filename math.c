#include <math.h>
#include <stdio.h>

int find_line_equation(double x1, double y1, double x2, double y2)
{
    double slope = (y2 - y1) / (x2 - x1);
        if((j - y1) == (slope * (i - x1)))
}

int main()
{
    double i = 0;
    while(i < 100)
        find_line_equation(0,0,300,300);
}